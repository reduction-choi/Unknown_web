function Loading({username}) {
    return (
        <div>
            <h1>Hi! {username}</h1>
            <h2>로딩 중...</h2>
            <h2>(무료 서버 사용중이라 좀 많이 느림)</h2>
        </div>
    )
}
export default Loading;